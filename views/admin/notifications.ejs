<%- include('../partials/header', { title: 'Kirim Notifikasi' }) %>

<h1><i class="fas fa-paper-plane"></i> Kirim Notifikasi ke Pengguna</h1>
<p>Kirim pesan, update, atau promo langsung ke lonceng notifikasi pengguna.</p>

<div class="card">
    <form action="/admin/notifications/send" method="POST">
        <div class="form-group">
            <label for="userId">Kirim Ke:</label>
            <select name="userId" id="userId" class="form-control" required>
                <option value="" disabled selected>-- Pilih Pengguna --</option>
                <% users.forEach(targetUser => { %>
                    <option value="<%= targetUser._id %>"><%= targetUser.name %> (<%= targetUser.email %>)</option>
                <% }) %>
            </select>
        </div>
        <div class="form-group">
            <label for="title">Judul Notifikasi</label>
            <input type="text" name="title" id="title" class="form-control" required placeholder="Contoh: Perpanjangan Domain Anda">
        </div>
        <div class="form-group">
            <label for="message">Pesan Singkat</label>
            <textarea name="message" id="message" class="form-control" rows="3" required placeholder="Contoh: Domain example.com akan segera kedaluwarsa."></textarea>
        </div>
        <div class="form-group">
            <label for="link">Link Tujuan (saat diklik)</label>
            <input type="text" name="link" id="link" class="form-control" placeholder="Contoh: /dashboard/domain/12345/manage">
        </div>
        <button type="submit" class="btn btn-primary" style="width: 100%;">
            <i class="fas fa-paper-plane"></i> Kirim Notifikasi
        </button>
    </form>
</div>

<%- include('../partials/footer') %>